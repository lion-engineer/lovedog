import { SterilizationPipe } from './sterilization.pipe';

describe('SterilizationPipe', () => {
  it('create an instance', () => {
    const pipe = new SterilizationPipe();
    expect(pipe).toBeTruthy();
  });
});
